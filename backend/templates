<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Social Media Sentiment Analyzer</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        :root {
            --primary: #4361ee;
            --secondary: #3f37c9;
            --accent: #4895ef;
            --dark: #1b263b;
            --light: #f8f9fa;
            --success: #4cc9f0;
            --danger: #f72585;
            --warning: #f8961e;
        }
        
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }
        
        body {
            background-color: #f5f7fa;
            color: var(--dark);
            line-height: 1.6;
        }
        
        .container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 20px;
        }
        
        header {
            background: linear-gradient(135deg, var(--primary), var(--secondary));
            color: white;
            padding: 2rem 0;
            text-align: center;
            border-radius: 0 0 20px 20px;
            box-shadow: 0 4px 20px rgba(0, 0, 0, 0.1);
            margin-bottom: 2rem;
        }
        
        h1 {
            font-size: 2.5rem;
            margin-bottom: 0.5rem;
        }
        
        .subtitle {
            font-size: 1.1rem;
            opacity: 0.9;
            font-weight: 300;
        }
        
        .dashboard {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 2rem;
            margin-bottom: 2rem;
        }
        
        @media (max-width: 768px) {
            .dashboard {
                grid-template-columns: 1fr;
            }
        }
        
        .card {
            background: white;
            border-radius: 15px;
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.05);
            padding: 1.5rem;
            transition: transform 0.3s ease, box-shadow 0.3s ease;
        }
        
        .card:hover {
            transform: translateY(-5px);
            box-shadow: 0 10px 25px rgba(0, 0, 0, 0.1);
        }
        
        .card-header {
            display: flex;
            align-items: center;
            justify-content: space-between;
            margin-bottom: 1.5rem;
            padding-bottom: 0.75rem;
            border-bottom: 1px solid #eee;
        }
        
        .card-title {
            font-size: 1.3rem;
            font-weight: 600;
            color: var(--dark);
            display: flex;
            align-items: center;
            gap: 0.5rem;
        }
        
        .platform-icon {
            font-size: 1.5rem;
        }
        
        .twitter-icon {
            color: #1DA1F2;
        }
        
        .instagram-icon {
            color: #E1306C;
        }
        
        .btn {
            background: var(--primary);
            color: white;
            border: none;
            padding: 0.5rem 1rem;
            border-radius: 50px;
            cursor: pointer;
            font-weight: 500;
            transition: all 0.3s ease;
            display: inline-flex;
            align-items: center;
            gap: 0.5rem;
        }
        
        .btn:hover {
            background: var(--secondary);
            transform: translateY(-2px);
        }
        
        .btn i {
            font-size: 0.9rem;
        }
        
        .sentiment-item {
            padding: 1rem;
            margin-bottom: 1rem;
            border-radius: 10px;
            background: #f8f9fa;
            position: relative;
            overflow: hidden;
        }
        
        .sentiment-item::before {
            content: '';
            position: absolute;
            left: 0;
            top: 0;
            height: 100%;
            width: 5px;
        }
        
        .positive::before {
            background: var(--success);
        }
        
        .negative::before {
            background: var(--danger);
        }
        
        .neutral::before {
            background: var(--warning);
        }
        
        .sentiment-text {
            margin-bottom: 0.5rem;
        }
        
        .sentiment-label {
            display: inline-block;
            padding: 0.25rem 0.75rem;
            border-radius: 50px;
            font-size: 0.8rem;
            font-weight: 600;
            text-transform: uppercase;
            letter-spacing: 0.5px;
        }
        
        .positive .sentiment-label {
            background: rgba(76, 201, 240, 0.2);
            color: var(--success);
        }
        
        .negative .sentiment-label {
            background: rgba(247, 37, 133, 0.2);
            color: var(--danger);
        }
        
        .neutral .sentiment-label {
            background: rgba(248, 150, 30, 0.2);
            color: var(--warning);
        }
        
        .stats {
            display: grid;
            grid-template-columns: repeat(3, 1fr);
            gap: 1.5rem;
            margin-bottom: 2rem;
        }
        
        .stat-card {
            background: white;
            border-radius: 15px;
            padding: 1.5rem;
            text-align: center;
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.05);
        }
        
        .stat-value {
            font-size: 2.5rem;
            font-weight: 700;
            margin-bottom: 0.5rem;
            background: linear-gradient(135deg, var(--primary), var(--accent));
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
        }
        
        .stat-label {
            color: #6c757d;
            font-size: 0.9rem;
            text-transform: uppercase;
            letter-spacing: 1px;
        }
        
        .loading {
            display: flex;
            justify-content: center;
            align-items: center;
            height: 200px;
        }
        
        .spinner {
            width: 40px;
            height: 40px;
            border: 4px solid rgba(0, 0, 0, 0.1);
            border-radius: 50%;
            border-top-color: var(--primary);
            animation: spin 1s ease-in-out infinite;
        }
        
        @keyframes spin {
            to { transform: rotate(360deg); }
        }
        
        footer {
            text-align: center;
            padding: 2rem 0;
            color: #6c757d;
            font-size: 0.9rem;
        }
    </style>
</head>
<body>
    <header>
        <div class="container">
            <h1>Social Media Sentiment Analyzer</h1>
            <p class="subtitle">Analyze public sentiment from Twitter and Instagram in real-time</p>
        </div>
    </header>
    
    <div class="container">
        <div class="stats">
            <div class="stat-card">
                <div class="stat-value" id="total-posts">0</div>
                <div class="stat-label">Total Posts Analyzed</div>
            </div>
            <div class="stat-card">
                <div class="stat-value" id="positive-posts">0</div>
                <div class="stat-label">Positive Sentiment</div>
            </div>
            <div class="stat-card">
                <div class="stat-value" id="negative-posts">0</div>
                <div class="stat-label">Negative Sentiment</div>
            </div>
        </div>
        
        <div class="dashboard">
            <div class="card">
                <div class="card-header">
                    <h2 class="card-title">
                        <i class="fab fa-twitter platform-icon twitter-icon"></i>
                        Twitter Analysis
                    </h2>
                    <button class="btn" id="analyze-twitter">
                        <i class="fas fa-sync-alt"></i> Analyze
                    </button>
                </div>
                <div id="twitter-results">
                    <div class="loading">
                        <div class="spinner"></div>
                    </div>
                </div>
            </div>
            
            <div class="card">
                <div class="card-header">
                    <h2 class="card-title">
                        <i class="fab fa-instagram platform-icon instagram-icon"></i>
                        Instagram Analysis
                    </h2>
                    <button class="btn" id="analyze-instagram">
                        <i class="fas fa-sync-alt"></i> Analyze
                    </button>
                </div>
                <div id="instagram-results">
                    <div class="loading">
                        <div class="spinner"></div>
                    </div>
                </div>
            </div>
        </div>
    </div>
    
    <footer>
        <div class="container">
            <p>© 2023 Social Media Sentiment Analyzer. All rights reserved.</p>
        </div>
    </footer>

    <script>
        document.addEventListener('DOMContentLoaded', function() {
            // Twitter analysis
            document.getElementById('analyze-twitter').addEventListener('click', function() {
                const twitterResults = document.getElementById('twitter-results');
                twitterResults.innerHTML = '<div class="loading"><div class="spinner"></div></div>';
                
                fetch('/analyze/twitter')
                    .then(response => response.json())
                    .then(data => {
                        updateResults('twitter-results', data);
                        updateStats(data);
                    })
                    .catch(error => {
                        twitterResults.innerHTML = '<p class="error">Error loading Twitter data</p>';
                        console.error('Error:', error);
                    });
            });
            
            // Instagram analysis
            document.getElementById('analyze-instagram').addEventListener('click', function() {
                const instagramResults = document.getElementById('instagram-results');
                instagramResults.innerHTML = '<div class="loading"><div class="spinner"></div></div>';
                
                fetch('/analyze/instagram')
                    .then(response => response.json())
                    .then(data => {
                        updateResults('instagram-results', data);
                        updateStats(data);
                    })
                    .catch(error => {
                        instagramResults.innerHTML = '<p class="error">Error loading Instagram data</p>';
                        console.error('Error:', error);
                    });
            });
            
            // Auto-load Twitter data on page load
            document.getElementById('analyze-twitter').click();
            
            function updateResults(elementId, data) {
                const element = document.getElementById(elementId);
                
                if (data.length === 0) {
                    element.innerHTML = '<p>No data available</p>';
                    return;
                }
                
                let html = '';
                data.forEach(item => {
                    html += `
                        <div class="sentiment-item ${item.sentiment.toLowerCase()}">
                            <p class="sentiment-text">${item.text}</p>
                            <span class="sentiment-label">${item.sentiment}</span>
                        </div>
                    `;
                });
                
                element.innerHTML = html;
            }
            
            function updateStats(data) {
                // Update total posts
                const currentTotal = parseInt(document.getElementById('total-posts').textContent);
                document.getElementById('total-posts').textContent = currentTotal + data.length;
                
                // Update sentiment counts
                data.forEach(item => {
                    const elementId = `${item.sentiment.toLowerCase()}-posts`;
                    const currentCount = parseInt(document.getElementById(elementId).textContent);
                    document.getElementById(elementId).textContent = currentCount + 1;
                });
            }
        });
    </script>
</body>
</html>
